{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Debug (LLDB)",
            "type": "lldb-dap",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
